---
phase: 10-more-controls
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - src/main/java/com/dwc/laf/DwcComponentGallery.java
autonomous: false

must_haves:
  truths:
    - "Gallery displays all 5 new components (tooltip, progress bar, scroll bar, tree, table) alongside existing 8"
    - "JProgressBar shows all 5 color variants in the gallery"
    - "JTable shows sample data with visible row striping and selection"
    - "JTree shows expandable nodes with custom chevron icons"
    - "Tooltip is demonstrated via hover instruction text"
    - "Gallery scrollbar is itself styled by DwcScrollBarUI"
  artifacts:
    - path: "src/main/java/com/dwc/laf/DwcComponentGallery.java"
      provides: "Updated gallery with 13-component showcase"
      contains: "createProgressBarSection"
  key_links:
    - from: "DwcComponentGallery.java"
      to: "DwcProgressBarUI"
      via: "dwc.progressType client property on progress bars"
      pattern: "dwc.progressType"
    - from: "DwcComponentGallery.java"
      to: "DwcTableUI"
      via: "JTable with sample data in gallery"
      pattern: "new JTable"
    - from: "DwcComponentGallery.java"
      to: "DwcTreeUI"
      via: "JTree with sample model in gallery"
      pattern: "new JTree"
---

<objective>
Update DwcComponentGallery to showcase all 5 new components, then verify visually.

Purpose: The gallery is the project's visual proof. Adding sections for tooltip, progress bar, scrollbar, tree, and table demonstrates all Phase 10 components work correctly with the L&F.

Output: Updated gallery showcasing 13 themed components total (8 existing + 5 new).
</objective>

<execution_context>
@/Users/beff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/beff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-more-controls/10-RESEARCH.md
@.planning/phases/10-more-controls/10-01-SUMMARY.md
@.planning/phases/10-more-controls/10-02-SUMMARY.md
@.planning/phases/10-more-controls/10-03-SUMMARY.md
@src/main/java/com/dwc/laf/DwcComponentGallery.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 new component sections to DwcComponentGallery</name>
  <files>
    src/main/java/com/dwc/laf/DwcComponentGallery.java
  </files>
  <action>
    Update DwcComponentGallery.java to add 5 new sections after the existing 8. The gallery title should be updated from "All 8 themed components" to "All 13 themed components".

    Add sections in this order (after TabbedPane section):

    1. **createProgressBarSection()**: JProgressBar showcase
       - Section titled "JProgressBar"
       - Horizontal layout with multiple bars
       - Default progress bar at 60% value with `setStringPainted(true)`
       - Success variant: `pb.putClientProperty("dwc.progressType", "success")`, value=75
       - Danger variant: value=30
       - Warning variant: value=50
       - Info variant: value=90
       - Indeterminate bar: `pb.setIndeterminate(true)`
       - Disabled bar: `pb.setEnabled(false)`, value=40
       - Use a vertical BoxLayout for the section, with FlowLayout rows for grouping (e.g., one row for default+disabled, one for variants, one for indeterminate)
       - Each bar: `new JProgressBar(0, 100)`, `pb.setValue(N)`, `pb.setPreferredSize(new Dimension(200, 12))` for thin look

    2. **createScrollBarSection()**: JScrollBar showcase
       - Section titled "JScrollBar"
       - Note text: "The gallery's own scroll bar is already styled. Below are standalone bars:"
       - One vertical JScrollBar (height ~100px) and one horizontal JScrollBar (width ~200px)
       - Set `sb.setPreferredSize(new Dimension(10, 100))` for vertical, `new Dimension(200, 10)` for horizontal
       - Both with `setValues(50, 10, 0, 100)` for visible thumb

    3. **createTreeSection()**: JTree showcase
       - Section titled "JTree"
       - Create a DefaultMutableTreeNode hierarchy:
         - Root: "Project"
           - "src" -> "main" -> "java", "resources"
           - "tests" -> "unit", "integration"
           - "docs" -> "README", "CHANGELOG"
       - `JTree tree = new JTree(root)`
       - `tree.setPreferredSize(new Dimension(300, 180))`
       - Expand the first couple of levels: `tree.expandRow(0)`, `tree.expandRow(1)`
       - Select row 3 to show selection highlighting: `tree.setSelectionRow(3)`

    4. **createTableSection()**: JTable showcase
       - Section titled "JTable"
       - Sample data: 6 rows x 4 columns
         - Columns: "Name", "Type", "Size", "Modified"
         - Rows: sample file-list data like `{"README.md", "File", "2 KB", "2026-02-10"}` etc.
       - `JTable table = new JTable(data, columnNames)`
       - Select row 1 to show selection: `table.setRowSelectionInterval(1, 1)`
       - Wrap in JScrollPane for header visibility: `JScrollPane tableScroll = new JScrollPane(table)`
       - `tableScroll.setPreferredSize(new Dimension(600, 180))`
       - `tableScroll.setAlignmentX(Component.LEFT_ALIGNMENT)`

    5. **createToolTipSection()**: JToolTip showcase
       - Section titled "JToolTip"
       - Since tooltips appear on hover, create a label with instructions:
         - JLabel with text "Hover over the buttons below to see themed tooltips"
       - Three buttons with tooltips:
         - `JButton b1 = new JButton("Hover me")`, `b1.setToolTipText("This is a themed tooltip with rounded corners and shadow")`
         - `JButton b2 = new JButton("Information")`, `b2.setToolTipText("Tooltips use DWC CSS tokens for consistent styling")`
         - `JButton b3 = new JButton("Disabled")`, `b3.setToolTipText("Even disabled buttons show styled tooltips")`, `b3.setEnabled(false)`

    Update the main method to add new sections:
    ```java
    main.add(Box.createVerticalStrut(24));
    main.add(createProgressBarSection());
    main.add(Box.createVerticalStrut(24));
    main.add(createScrollBarSection());
    main.add(Box.createVerticalStrut(24));
    main.add(createTreeSection());
    main.add(Box.createVerticalStrut(24));
    main.add(createTableSection());
    main.add(Box.createVerticalStrut(24));
    main.add(createToolTipSection());
    ```

    Update subtitle text to "All 13 themed components. Hover and press to see interactive states."

    Ensure all new imports are added: `javax.swing.tree.DefaultMutableTreeNode`, `javax.swing.JTree`, `javax.swing.JTable`, `javax.swing.JScrollBar`, `javax.swing.JProgressBar`, `javax.swing.JToolTip`.
  </action>
  <verify>
    `cd /Users/beff/_lab/dwclaf && mvn compile -q` -- compiles without errors.
    `cd /Users/beff/_lab/dwclaf && mvn test -q` -- all tests still pass.
  </verify>
  <done>
    DwcComponentGallery updated with 5 new sections showing all Phase 10 components: progress bars with 5 color variants + indeterminate + disabled, standalone scrollbars, tree with expanded nodes and selection, table with sample data and row striping, and tooltip demo buttons. Total: 13 component sections.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all 13 components in gallery</name>
  <what-built>
    Complete Phase 10: five new themed UI delegates (JToolTip, JProgressBar, JScrollBar, JTree, JTable) integrated into DwcComponentGallery. The gallery now showcases 13 themed Swing components.
  </what-built>
  <how-to-verify>
    1. Run: `cd /Users/beff/_lab/dwclaf && mvn compile exec:java -q`
    2. Scroll through the gallery and verify:
       - **Progress bars**: Default (blue), success (green), danger (red), warning (amber), info (blue variant) all show distinct colors. Indeterminate bar animates. Disabled bar is dimmed.
       - **Scroll bars**: The gallery's own scrollbar is thin and modern. Standalone scrollbars show rounded thumb.
       - **Tree**: Custom chevron icons (right arrow for collapsed, down arrow for expanded). Selected node shows blue highlight. No connecting lines.
       - **Table**: Alternating row stripes visible. Selected row shows blue highlight with white text. Header has bold text with bottom separator line. No grid lines.
       - **Tooltips**: Hover over the tooltip demo buttons. Tooltips should have rounded corners, shadow, and themed background/text colors.
    3. Try the theme switcher dropdown -- all new components should re-theme correctly.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `mvn compile exec:java` launches gallery with all 13 component sections
- All 5 new components render with DWC theme styling
- Theme switcher re-applies L&F and all new components update
- `mvn test -q` passes all tests
</verification>

<success_criteria>
- Gallery shows progress bar section with 5 color variants + indeterminate + disabled
- Gallery shows scrollbar section with vertical and horizontal bars
- Gallery shows tree section with custom chevron icons and selection
- Gallery shows table section with row striping, selection, and styled header
- Gallery shows tooltip section with hover demo
- Theme switcher works across all 13 components
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/10-more-controls/10-04-SUMMARY.md`
</output>
